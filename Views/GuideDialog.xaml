<Window x:Class="HostManager.Views.GuideDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="사용 가이드" 
        Height="600" 
        Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FAFAFA"
        WindowStyle="None"
        AllowsTransparency="True">
    
    <Window.Resources>
        <!-- Colors -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2196F3"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#1976D2"/>
        <SolidColorBrush x:Key="PrimaryLightBrush" Color="#BBDEFB"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#FAFAFA"/>
        <SolidColorBrush x:Key="DividerBrush" Color="#BDBDBD"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#212121"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#757575"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#4CAF50"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#FF9800"/>
    </Window.Resources>
    
    <Border Background="{StaticResource SurfaceBrush}" 
            CornerRadius="8" 
            BorderBrush="{StaticResource DividerBrush}" 
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="20" ShadowDepth="4"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>
            
            <!-- 타이틀 바 -->
            <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="📖 Host Manager 사용 가이드" 
                               Foreground="White" 
                               FontSize="16" 
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               Margin="20,0,0,0"/>
                    <Button Content="✕" 
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0,0,10,0"
                            Background="Transparent"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="16"
                            Width="30" Height="30"
                            Cursor="Hand"
                            Click="CloseButton_Click"/>
                </Grid>
            </Border>
            
            <!-- 가이드 내용 -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24,16">
                <StackPanel>
                    <!-- 프로그램 소개 -->
                    <TextBlock Text="프로그램 소개" 
                               FontSize="18" 
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}"
                               Margin="0,0,0,8"/>
                    <TextBlock TextWrapping="Wrap" 
                               FontSize="13"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               Margin="0,0,0,20">
                        Host Manager는 Windows hosts 파일을 쉽게 관리할 수 있는 도구입니다.
                        환경별, 그룹별로 호스트를 분류하여 관리하고, 활성화/비활성화를 통해 
                        개발 환경을 빠르게 전환할 수 있습니다.
                    </TextBlock>
                    
                    <!-- 주요 기능 -->
                    <TextBlock Text="🚀 주요 기능" 
                               FontSize="18" 
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}"
                               Margin="0,0,0,8"/>
                    <Border Background="{StaticResource BackgroundBrush}" 
                            CornerRadius="8" 
                            Padding="16" 
                            Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="• 호스트 추가/삭제/수정" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock Text="• 환경별 분류 (local, dev, staging, prod 등)" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock Text="• 그룹별 분류 (개발, 운영, DB 등)" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock Text="• 호스트 활성화/비활성화 토글" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock Text="• IP, 호스트명 검색 기능" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock Text="• 변경 내용 하이라이트 (수정: 주황색, 신규: 초록색)" FontSize="13"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- 키보드 단축키 -->
                    <TextBlock Text="⌨️ 키보드 단축키" 
                               FontSize="18" 
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}"
                               Margin="0,0,0,8"/>
                    <Border Background="{StaticResource BackgroundBrush}" 
                            CornerRadius="8" 
                            Padding="16" 
                            Margin="0,0,0,20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Ctrl + H" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,6"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="가이드 열기" FontSize="13" Margin="0,0,0,6"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Ctrl + R / F5" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,6"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="새로고침" FontSize="13" Margin="0,0,0,6"/>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Ctrl + S" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,6"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="저장" FontSize="13" Margin="0,0,0,6"/>
                            
                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Ctrl + N" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,6"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="새 호스트 추가" FontSize="13" Margin="0,0,0,6"/>
                            
                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Delete" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,6"/>
                            <TextBlock Grid.Row="4" Grid.Column="1" Text="선택한 호스트 삭제" FontSize="13" Margin="0,0,0,6"/>
                            
                            <TextBlock Grid.Row="5" Grid.Column="0" Text="Ctrl + A" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,6"/>
                            <TextBlock Grid.Row="5" Grid.Column="1" Text="전체 선택/해제" FontSize="13" Margin="0,0,0,6"/>
                            
                            <TextBlock Grid.Row="6" Grid.Column="0" Text="Ctrl + F" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,6"/>
                            <TextBlock Grid.Row="6" Grid.Column="1" Text="검색창으로 이동" FontSize="13" Margin="0,0,0,6"/>
                            
                            <TextBlock Grid.Row="7" Grid.Column="0" Text="Ctrl + 1" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,6"/>
                            <TextBlock Grid.Row="7" Grid.Column="1" Text="환경 드롭박스 열기/닫기" FontSize="13" Margin="0,0,0,6"/>
                            
                            <TextBlock Grid.Row="8" Grid.Column="0" Text="Ctrl + 2" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Grid.Row="8" Grid.Column="1" Text="그룹 드롭박스 열기/닫기" FontSize="13"/>
                        </Grid>
                    </Border>
                    
                    <!-- 사용 방법 -->
                    <TextBlock Text="📝 사용 방법" 
                               FontSize="18" 
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}"
                               Margin="0,0,0,8"/>
                    <Border Background="{StaticResource BackgroundBrush}" 
                            CornerRadius="8" 
                            Padding="16" 
                            Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="1. 호스트 추가" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock Text="   '추가' 버튼 또는 Ctrl+N을 눌러 새 호스트를 추가합니다." FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                            
                            <TextBlock Text="2. 호스트 편집" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock Text="   테이블에서 직접 IP, 호스트명, 환경, 그룹, 설명을 수정합니다." FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                            
                            <TextBlock Text="3. 저장" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock Text="   변경 후 '저장' 버튼 또는 Ctrl+S로 hosts 파일에 저장합니다." FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                            
                            <TextBlock Text="4. 활성화/비활성화" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock Text="   체크박스로 선택 후 '활성화' 또는 '비활성화' 버튼을 클릭합니다." FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                            
                            <TextBlock Text="5. 필터링" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock Text="   환경/그룹 드롭박스 또는 검색창으로 호스트를 필터링합니다." FontSize="13" Foreground="{StaticResource TextSecondaryBrush}"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- 주의사항 -->
                    <TextBlock Text="⚠️ 주의사항" 
                               FontSize="18" 
                               FontWeight="Bold"
                               Foreground="{StaticResource WarningBrush}"
                               Margin="0,0,0,8"/>
                    <Border Background="#FFF3E0" 
                            CornerRadius="8" 
                            Padding="16" 
                            Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="• 이 프로그램은 관리자 권한으로 실행해야 합니다." FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock Text="• hosts 파일 수정 전 백업을 권장합니다." FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock Text="• 잘못된 hosts 설정은 네트워크 문제를 일으킬 수 있습니다." FontSize="13"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- 백업 안내 -->
                    <TextBlock Text="💾 백업 파일 안내" 
                               FontSize="18" 
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}"
                               Margin="0,0,0,8"/>
                    <Border Background="{StaticResource BackgroundBrush}" 
                            CornerRadius="8" 
                            Padding="16" 
                            Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="• hosts_prev_backup" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock Text="   최초 실행 시 또는 Host Manager 형식이 아닌 기존 hosts 파일을 자동 백업합니다." 
                                       FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8" TextWrapping="Wrap"/>
                            
                            <TextBlock Text="• hosts_backup" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock Text="   '백업' 버튼 클릭 시 현재 hosts 파일을 수동으로 백업합니다." 
                                       FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8" TextWrapping="Wrap"/>
                            
                            <TextBlock Text="※ 백업 파일 위치: C:\Windows\System32\drivers\etc\" 
                                       FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" FontStyle="Italic"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- hosts 파일 예시 -->
                    <TextBlock Text="📄 hosts 파일 예시" 
                               FontSize="18" 
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}"
                               Margin="0,0,0,8"/>
                    <Border Background="#263238" 
                            CornerRadius="8" 
                            Padding="16" 
                            Margin="0,0,0,10">
                        <TextBlock FontFamily="Consolas" 
                                   FontSize="12" 
                                   Foreground="#E0E0E0"
                                   TextWrapping="NoWrap">
                            <Run Foreground="#78909C"># [local] 로컬 개발 환경</Run><LineBreak/>
                            <Run Foreground="#80CBC4">127.0.0.1</Run><Run>       </Run><Run Foreground="#FFCC80">local.myapp.com</Run><Run Foreground="#78909C">       # 로컬 앱 서버</Run><LineBreak/>
                            <Run Foreground="#80CBC4">127.0.0.1</Run><Run>       </Run><Run Foreground="#FFCC80">local.api.com</Run><Run Foreground="#78909C">         # 로컬 API</Run><LineBreak/>
                            <LineBreak/>
                            <Run Foreground="#78909C"># [dev] 개발 서버</Run><LineBreak/>
                            <Run Foreground="#80CBC4">192.168.1.100</Run><Run>   </Run><Run Foreground="#FFCC80">dev.myapp.com</Run><Run Foreground="#78909C">         # 개발 서버</Run><LineBreak/>
                            <Run Foreground="#78909C">#192.168.1.101   dev.api.com           # 비활성화됨</Run><LineBreak/>
                            <LineBreak/>
                            <Run Foreground="#78909C"># [prod] 운영 서버</Run><LineBreak/>
                            <Run Foreground="#80CBC4">10.0.0.50</Run><Run>       </Run><Run Foreground="#FFCC80">prod.myapp.com</Run><Run Foreground="#78909C">        # 운영 서버</Run>
                        </TextBlock>
                    </Border>
                </StackPanel>
            </ScrollViewer>
            
            <!-- 버튼 -->
            <Border Grid.Row="2" BorderBrush="{StaticResource DividerBrush}" BorderThickness="0,1,0,0">
                <Button Content="확인" 
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Background="{StaticResource PrimaryBrush}"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="40,10"
                        FontSize="13"
                        Cursor="Hand"
                        Click="CloseButton_Click"/>
            </Border>
        </Grid>
    </Border>
</Window>
